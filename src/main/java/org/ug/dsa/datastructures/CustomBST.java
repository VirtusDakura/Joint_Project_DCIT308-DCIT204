package org.ug.dsa.datastructures;

/**
 * Custom Binary Search Tree for tree-based indexing over locations and requests.
 *
 * Assigned to: Jessica Zunuo Puozaa (22120404)
 *
 * Required operations:
 *   - insert(K key, V value)
 *   - search(K key)           : Return value or null
 *   - delete(K key)
 *   - inorderTraversal()      : Print keys in sorted order
 *   - preorderTraversal(), postorderTraversal()
 *   - minimum(), maximum()
 *   - height(), size()
 *
 * Evidence to produce:
 *   - Search path trace for a specific key
 *   - Sorted inorder output demonstration
 *   - Unit tests for insert, search hit/miss, delete leaf/one-child/two-children,
 *     duplicate key, empty tree, single node
 */
public class CustomBST<K extends Comparable<K>, V> {

    // TODO: Implement the BST node class, root pointer, and all required methods.
    // Hint: Each node holds a key, value, and left/right child references.

}
